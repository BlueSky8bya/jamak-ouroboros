# 연습 영상 재설계 계획 (2026-07-16)

## 원칙 (정책 2026-07-16)
- 연습 영상은 **무-API 도구만** 가르친다. API 쓰는 것(맞춤법 검사·번역 생성·초안)은 관리자 전용이라 **아예 안 가르침**.
- 실제 검수에서 **숨긴 도구**(안심 확인·복구 채우기)도 안 가르침 — 배워도 실전에서 못 씀.
- 스텝 내용을 바꾸려면 **영상 재렌더 필수**(나레이션이 교재라 코드만 바꾸면 어긋남).

## 6개 코스 — 무엇을 가리킬지

### 연습1 · 기본기 (basic) — **변경 없음, 재렌더 X**
영상 재생 → 위에서부터 Enter 확인 → 틀린 글자 고치고 Enter → 🙉 보류 → 되돌리기(Alt+Z) → 큰 글씨.
→ 전부 무-API. 그대로 둔다.

### 연습2 · 재생 다루기 (playback) — **변경 없음, 재렌더 X**
Tab 재생/멈춤 → 3초 뒤로 → 배속(느리게) → 3초 앞 → 10초 이동 → 이 자막 처음부터 → 구간 반복 → 자막 사이 이동.
→ 순수 키보드, 무-API. 그대로.

### 연습3 · 빠르게 훑기 (fast) — **변경 O, 재렌더 O**
- ❌ 삭제: **✅ 안심 확인** (실검수에서 숨김 — 배지가 오타 못 거름)
- ✅ 유지: 🔎 찾기·바꾸기(같은 오타 일괄) · '편집 시작 시 멈춤' 끄기 · '자동 따라가기' 설정 · 미확인만 순회
- → 리드를 **찾기·바꾸기**로 시작하도록 재구성.

### 연습4 · 나누기·합치기 (structure) — **변경 없음, 재렌더 X**
자막 글 선택 → 나누기 → 합치기 → 되돌리기.
→ 무-API. 그대로.

### 연습5 · 타이밍 (timing) — **변경 없음, 재렌더 X**
② 타이밍 탭 → ✨ 자동 정리 → 문제만 순회 → Alt+[ 시작 · Alt+] 넘김 · Alt+\ 발화 맞춤 → ✂ 무음 다듬기 → 타임라인 손잡이 끌기 → ⏱ 타이밍 검수 완료.
→ 전부 무-API. 그대로.

### 연습6 · 마무리 (finish) — **변경 O, 재렌더 O**
- ✅ 유지: 💬 미리보기 모드(Alt+P)
- ❌ 삭제: **🛠 복구·채우기** (파이프라인이 자동 처리 + 실검수 숨김)
- ➕ 추가: **漢 한자 채우기** (핵심 신규 무-API 도구)
  - 漢 눌러 → 미리보기 목록 → 각 행 **▶ 시각**을 눌러 그 부분 영상 재생
  - **흑판에 쓴 한자인지 / 동음이의 중 맞는지** 들으며 판단 → 맞으면 체크, 아니면 해제 → 적용
  - 함정 예: "영상을 **보시는**" 의 보시 ≠ 布施 (일상어) → ▶ 들어보고 체크 해제
- ✅ 유지: 📚 학습(Alt+K) · 자막 받기(점검표)
- 🔧 수정: 마지막 나레이션의 **"✏️ 맞춤법 검사" 언급 삭제** (관리자 전용이라 검수자에겐 없음)

## 재렌더 대상: 연습3 · 연습6 (2개). 1·2·4·5는 그대로.

## 실행 순서 (재렌더 2개)
1. 대본 수정 — `scripts/연습3-빠르게훑기.md`, `연습6-마무리.md` (안심·복구·맞춤법 대사 제거, 연습6에 한자 채우기 대사 추가)
2. 투어 스텝 수정 — `Editor.tsx` COURSES fast/finish + `tutorialSync.ts` 체크포인트·TUTORIAL_LINES
3. 결함 주입 재설계 — `practice.py` 연습6 클론에 한자 병기 대상(용맹정진·사성제 등) + 동음이의 함정(보시) 심기
4. 렌더 — edge-tts + Remotion → 연습3·6 mp4
5. 업로드(사용자)·워커 처리·🎓 지정 → 새 video_id로 코스 재바인딩
6. E2E 검증(스텝↔영상 싱크, ▶ 미리보기 동작) + 배포

## 열린 결정
- **연습3도 재렌더할지**: 안심 확인만 빼는 거라 실익 작음. 비용 아끼려면 연습6만 재렌더하고 연습3는 유지(안심확인은 practice 모드에 남아 투어는 안 깨짐). → 사용자 선택.
