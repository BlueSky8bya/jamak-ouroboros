# 코스 × 대본 × 단축키 매핑

투어 단계(실제 동작을 해야 진행) ↔ 그 동작을 유도하는 대본 줄 ↔ 단축키/기능.
리허설 때 이 표로 "대본이 정말 그 조작을 유도하는지" 점검한다.

## 코스 1 · 기본기 (연습1-기본기.md)

| 투어 단계 (advance 이벤트) | 유도 대본 줄 | 단축키/기능 |
|---|---|---|
| ▶ 재생 클릭 (`play`) | #1~3 "귀로 들어 보세요" | ▶ 재생 |
| 확인 (`confirm`) | #4~6 쉬운 문장 + "맞으면 엔터" | Enter |
| 자막 글 클릭 (`open-row`) | #7~10 오인식 미끼(깻잎·밤나무·축지법·공중부양) | 클릭→고치기→Enter |
| 🙉 (`hold`) | #11 웅얼 문장 + #12 안내 | Alt+H / 🙉 |
| 되돌리기 (`undo`) | #13 "알트 제트" | Alt+Z / ↶ |
| 글씨 크게 (`bigtype`) | #14 "글씨 크게 버튼을 눌러 보세요" | 글씨 크게 버튼 |

## 코스 2 · 재생 다루기 (연습2-재생다루기.md)

| 투어 단계 | 유도 대본 줄 | 단축키/기능 |
|---|---|---|
| Tab (`play`) | #2 + 2.5초 여유 | Tab |
| 3초 뒤 (`seek-back`) | #4 속사포 → #5 안내 | Shift+Tab / Ctrl+← |
| 3초 앞 (`seek-fwd`) | #7 5초 침묵 + 안내 | Ctrl+→ |
| 10초 (`seek-10`) | #9 | Ctrl+Shift+←/→ |
| 구간 처음 (`replay`) | #10 (역슬래시 위치 안내 포함) | Ctrl+\ / ⏮ |
| 느리게 (`rate`) | #6 (#4가 동기) | 0.75× |
| 반복 (`loop`) | #11 | Alt+R / 🔁 |
| 자막 이동 (`nav`) | #12 "알트와 위아래 화살표" | Alt+↑↓ |

## 코스 3 · 빠르게 훑기 (연습3-빠르게훑기.md) — 2026-07-17 재설계

| 투어 단계 | 유도 대본 줄 | 단축키/기능 |
|---|---|---|
| Enter 촤르륵 (`confirm`) | #2~7 쉬운 상식 문장 5개 + "맞으면 엔터로 쭉" | Enter (untilTime) |
| 찾기·바꾸기 (`find`) | #8~11 "뭉치" 4회 + #12 | Alt+B / 🔎 |
| 멈춤 끄기 (`pausetype`) | #13 | Alt+S |
| 따라가기 (`follow`) | #14 | 🎧 토글 |
| 미확인 이동 (`nav-unreviewed`) | #15 | Alt+Shift+↑↓ / 이어서 |

❌ 삭제: ✅ 안심 확인(`confirm-safe`) — 실검수에서 숨긴 버튼.

## 코스 4 · 나누기·합치기 (연습4-나누기합치기.md)

| 투어 단계 | 유도 대본 줄 | 단축키/기능 |
|---|---|---|
| 글 클릭 (`open-row`) | #2 초장문 | 클릭 |
| 나누기 (`split`) | #3 | Ctrl+Enter |
| 되돌리기 (`undo`) | #9 | Alt+Z |
| 합치기 (`merge`) | #4~7 토막말(0.75s 간격) + #8 | Ctrl+Shift+Enter |
| 되돌리기 (`undo`) | #9 | Alt+Z |
| (언급만) | #10 박수/기침 | Alt+Delete |

## 코스 5 · 타이밍 (연습5-타이밍.md) — 2026-07-17 재설계

| 투어 단계 | 유도 대본 줄 | 단축키/기능 |
|---|---|---|
| ② 탭 (`mode-timing`) | #3 | ② 타이밍 탭 |
| 문제 큐 (`next-issue`) | #4 속사포(⏩ 유발) + #5 | 다음 문제 → |
| 여기서 시작 (`start-here`) | #6 "고치는 곳은 영상 바로 밑" + #7 | Alt+[ · **`.timing-bar`** |
| 여기서 넘김 (`next-here`) | #8 | Alt+] · `.timing-bar` |
| 발화 맞춤 (`set-times`) | #9 | Alt+\ · `.timing-bar` |
| 타임라인 (`edge-drag`) | #11 | 손잡이 드래그 |
| 무음 다듬기 (`tighten`) | #12 | Alt+M / ✂ |
| (언급만) | #10 ◀▶ 0.1초 넛지·숫자 직접 입력 · #13 ⏱ 완료 | `.timing-bar` |

❌ 삭제: ✨ 자동 정리(`auto-timing`) — ADR-0012로 폐지.
🔄 타깃 이동: 오른쪽 셀 `.timing-tools` → **영상 밑 `.timing-bar`** (v0.9.63).

## 코스 6 · 마무리 (연습6-마무리.md) — 2026-07-17 **전면 재설계: 실전 캡스톤**

나레이션이 곧 드릴 재료다 (이 영상의 자막 = 이 나레이션의 STT).

| 투어 단계 | 유도 대본 줄 | 단축키/기능 |
|---|---|---|
| 속도 정하기 (`pausetype`) | #2 "본인 속도가 정답" | Alt+S |
| Enter 촤르륵 (`confirm`) | #3~7 정확한 문장 4개 | Enter (untilTime) |
| 군소리 삭제 (`delete`) | #8 "음...", #9 "어." + #10 | **Alt+Delete** |
| 합치기 (`merge`) | #11+#12 (중간 쉼으로 끊어 말함) + #13 | Ctrl+Shift+Enter |
| 나누기 (`split`) | #14 (두 문장 연달아) + #15 | Ctrl+Enter → Enter |
| 되짚기 (`nav`) | #16 | Alt+↑↓ · Shift+Tab · Tab |
| 漢 채우기 (`hanja-fill`) | #17 해탈·열반 + #19 | 漢 · `.tool-hanja` |
| 모달에서 거르기 (`hanja-apply`) | #18 **"영상을 보시는"**(보시=布施 함정) + #19 | 행 ▶ → 안 바꿈 → 적용 |
| 미리보기 (`preview`) | #20 | Alt+P / 💬 |
| 학습 (`absorb`) | #21 | Alt+K / 📚 |
| 자막 받기 (`export-check`) | #22 | 자막 받기 → 점검표 |

❌ 삭제: 🛠 복구·채우기(실검수 숨김) · ✏️ 맞춤법 언급(관리자 전용).
🆕 신규 이벤트: `hanja-fill` · `hanja-apply` (v0.9.68).

## 커버리지 상태

주요 단축키·기능 전부 커버됨 (2026-07-14 보강: 글씨 크게→코스1, Alt+↑↓→코스2,
Alt+Shift+↑↓·이어서→코스3. 2차 보강: 컨트롤 화면 위치 안내(배속·체크 칸 줄·
도구 줄·찾기바꾸기·자막 받기), Shift+Tab 3초 뒤→연습2 #5, "타이밍은 글자 검수
후" 권장→연습5 #1, 미리보기 중 타임라인 경계 조절→연습6 #3. 총 77대사).

의도적 생략 (심화 — 코스로 안 가르침):
| 기능 | 이유 |
|---|---|
| 시간 ◀▶ 미세조정 · WordMap 드래그 | 코스5의 타임라인 드래그로 개념 갈음. 필요한 사람만 자연 발견 |
| 언어 트랙/포크/재번역 | 번역 검수자용 — 별도 코스 후보 (연습 영상 범위 밖) |
| 관리자 기능(다시 인식, .srt 임포트, 연습용 지정) | 관리자 1인용 — 튜토리얼 대상 아님 |
